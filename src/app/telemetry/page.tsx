import { TelemetryScreen } from "@/features/telemetry/components/TelemetryScreen";

export const metadata = {
  title: "Telemetry & Tracking - Carbon Ledger",
  description: "Real-time vehicle tracking and emissions monitoring.",
};

export default function TelemetryPage() {
  return <TelemetryScreen />;
}
